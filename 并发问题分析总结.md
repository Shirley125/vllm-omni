# Qwen3-Omni 并发请求卡住问题分析总结

## 问题描述

在应用 commit d0c7d56e (可能是 d0836d8) 之后,并发发送请求时推理会被卡住,并出现警告:
```
(Worker pid=51923) [Stage-2] WARNING 02-05 05:32:15 [qwen3_omni.py:362] 
Input_ids length: 1 is not divisible by 16, padding with zeros. 
This should only happen in warm up
```

## 根本原因

### 1. Commit d0836d8 引入的关键变更

该 commit 标题为 "[Bugfix] Fix multi-audio input shape alignment for Qwen3-Omni Thinker",主要改动:

**新增了 `_parse_and_validate_audio_input` 方法**:
```python
def _parse_and_validate_audio_input(self, **kwargs):
    # 将 [batch_size, feature_dim, chunk_size] 转换为 [feature_dim, batch_size * chunk_size]
    input_audio_features = input_audio_features.permute(1, 0, 2).flatten(1)
    # 将 [batch_size, seq_len] 重塑为 [batch_size * seq_len]
    audio_feature_lengths = audio_feature_lengths.reshape(-1)
```

**移除了 `aftercnn_lens` 参数**:
```python
# 之前的代码:
audio_outputs = self.audio_tower(
    input_features,
    feature_lens=audio_feature_lengths,
    aftercnn_lens=audio_feat_lengths,  # ← 这个参数被移除了
)

# commit 后:
audio_outputs = self.audio_tower(
    input_features,
    feature_lens=audio_feature_lengths,
    # aftercnn_lens 参数消失了
)
```

### 2. 为什么会导致并发请求卡住

#### 问题 1: 批次维度被展平,丢失了请求边界

```python
# 场景: 2个并发请求,音频长度不同

# 请求 1: 3.0秒音频 → 3000个采样点 → 处理后 187个token
# 请求 2: 2.5秒音频 → 2500个采样点 → 处理后 156个token

# 正常情况(没有 commit):
audio_features_1 = [187, hidden_dim]  # 请求1
audio_features_2 = [156, hidden_dim]  # 请求2
# 每个请求保持自己的边界

# 应用 commit 后(错误):
# 步骤1: 输入被批处理
input_audio = torch.stack([audio_1, audio_2])  # [2, 128, 3000]

# 步骤2: permute 和 flatten (丢失了批次边界)
input_audio = input_audio.permute(1, 0, 2).flatten(1)
# 形状: [128, 6000]  ← 无法知道请求1在哪里结束,请求2从哪里开始!

# 步骤3: audio_tower 处理时没有 aftercnn_lens
# 它不知道序列应该在位置 187 处分割
# 导致注意力掩码错误,特征对齐失败
```

#### 问题 2: 注意力掩码错位

```python
# 在 Qwen3OmniMoeAudioEncoder 中:

# 没有 aftercnn_lens 参数时:
# - 无法为批处理输入创建正确的注意力掩码
# - 所有序列被当作一个长序列处理
# - 请求之间的交叉注意力污染

# 例如:
# 请求1的 token: [0:187]
# 请求2的 token: [188:343]  # 应该是 [0:156] 但被偏移了

# 没有正确边界的注意力掩码:
# 请求1 会注意到请求2的token (错误!)
# 请求2的特征被请求1的长度偏移了 (数据损坏!)

# 结果:
# - 两个请求都得到被污染的特征
# - 下游阶段接收到垃圾数据
# - 编解码生成产生错误的或最小的codes
# - 流水线挂起或产生错误输出
```

#### 问题 3: Stage 2 (Code2Wav) 接收到错误的输入长度

```python
# 在 qwen3_omni.py 第440行:

elif self.model_stage == "code2wav":
    if input_ids is not None:
        codes.append(input_ids.reshape(1, 16, -1))  # 要求 input_ids 长度必须是 16 的倍数!

# 正常情况:
# input_ids = [2400]  → reshape → [1, 16, 150] ✓

# 出错情况(受 commit 影响):
# 由于上游 Stage 0 的音频特征错位
# Talker 可能输出错误的 codec codes
# input_ids = [1]  → 无法 reshape 到 [1, 16, -1]!
# 1 不能被 16 整除!
# 警告: "Input_ids length: 1 is not divisible by 16, padding with zeros"
```

### 3. 完整的错误流程

```
Stage 0 (Thinker) - 音频处理:
  请求1: 音频 [3000] ─┐
                      ├→ 批处理 [2, 128, 3000]
  请求2: 音频 [2500] ─┘
                ↓
        permute + flatten (错误!)
                ↓
        [128, 6000] (丢失边界)
                ↓
        audio_tower 没有 aftercnn_lens
                ↓
        特征错位,边界信息丢失
                ↓
Stage 1 (Talker) - 编码生成:
        接收到错位的特征
                ↓
        无法正确分割每个请求的特征
                ↓
        生成错误的 codec codes:
        请求1: 期望 [150, 16], 实际 [16, 1] 或 [1, 1]
        请求2: 期望 [120, 16], 实际 [1, 16] 或空
                ↓
Stage 1→2 转换:
        codec_codes.reshape(-1)
        请求1: [16] 或 [1] (应该是 [2400])
        请求2: [16] 或 [1] (应该是 [1920])
                ↓
Stage 2 (Code2Wav):
        input_ids.reshape(1, 16, -1)
        长度 1 或 16 无法正确 reshape
                ↓
        尝试填充,但数据已损坏
                ↓
        ⚠️ 警告: "length 1 is not divisible by 16"
                ↓
        流水线卡住,等待永远不会到来的正确输入
                ↓
        💥 DEADLOCK (死锁)
```

## 解决方案

### 方案 1: 恢复 aftercnn_lens 参数 (推荐)

修改 `vllm_omni/model_executor/models/qwen3_omni/qwen3_omni_moe_thinker.py`:

```python
class Qwen3OmniMoeConditionalGenerationMixin(Qwen2_5OmniConditionalGenerationMixin):
    def _parse_and_validate_audio_input(self, **kwargs: object):
        input_audio_features = kwargs.pop("input_audio_features", None)
        audio_feature_lengths = kwargs.pop("audio_feature_lengths", None)
        
        if input_audio_features is None:
            return None, None
        
        # 关键修改: 不要展平批次维度!
        # 保持 [batch_size, feature_dim, chunk_size] 格式
        if isinstance(input_audio_features, torch.Tensor):
            if input_audio_features.ndim == 2:
                input_audio_features = input_audio_features.unsqueeze(0)
        
        # 计算 aftercnn_lens (用于注意力掩码)
        audio_feat_lengths, audio_output_lengths = _get_feat_extract_output_lengths(
            audio_feature_lengths
        )
        
        return Qwen2_5OmniAudioFeatureInputs(
            type="audio_features",
            input_features=input_audio_features,
            audio_feature_lengths=audio_feature_lengths,
            feature_attention_mask=feature_attention_mask,
        ), audio_feat_lengths  # ← 返回这个参数!
    
    def _process_audio_input(
        self,
        audio_input: Qwen2_5OmniAudioFeatureInputs,
    ) -> torch.Tensor:
        input_features = audio_input["input_features"]
        audio_feature_lengths = audio_input["audio_feature_lengths"]
        
        audio_feat_lengths, audio_output_lengths = _get_feat_extract_output_lengths(
            audio_feature_lengths
        )
        
        # 关键修改: 恢复 aftercnn_lens 参数!
        audio_outputs = self.audio_tower(
            input_features.to(self.audio_tower.dtype),
            feature_lens=audio_feature_lengths,
            aftercnn_lens=audio_feat_lengths,  # ← 恢复这个参数!
        )
        audio_features = audio_outputs.last_hidden_state
        
        # 按边界正确分割
        return audio_features.split(audio_output_lengths.tolist())
```

### 方案 2: 在 Code2Wav 中添加长度验证

修改 `vllm_omni/model_executor/models/qwen3_omni/qwen3_omni.py`:

```python
elif self.model_stage == "code2wav":
    codes = []
    if input_ids is not None:
        # reshape 前验证长度
        if input_ids.numel() % 16 != 0:
            logger.error(
                f"[Stage-2] Code2Wav 接收到无效的 input_ids 长度: {input_ids.numel()}, "
                f"不能被 16 整除。这表明上游 Stage 数据损坏。"
                f"期望形状: [16*N], 实际得到: [{input_ids.numel()}]"
            )
            # 尝试恢复(可能产生错误的音频)
            pad_length = 16 - (input_ids.numel() % 16)
            input_ids = torch.nn.functional.pad(input_ids, (0, pad_length), value=0)
            logger.warning(f"[Stage-2] 填充了 {pad_length} 个零来修复形状")
        
        codes.append(input_ids.reshape(1, 16, -1))
```

### 方案 3: 直接回退 commit

```bash
# 最简单但需要重新测试原始bug
git revert d0836d8
```

## 与 vLLM v0.15.0 的关系

vLLM v0.15.0 中的关键特性影响此问题:

1. **Chunked Prefill (分块预填充)**: 允许将长预填充分解为块
2. **Paged Attention V2**: 要求块对齐的序列
3. **Multi-Step Scheduling (多步调度)**: 将多个请求批处理在一起

当音频输入长度不同且没有正确的 `aftercnn_lens` 时:
- 调度器无法正确分块输入
- 填充计算失败
- 并发批处理死锁

## 测试建议

```python
import asyncio
from vllm import AsyncLLMEngine

async def test_concurrent_requests():
    engine = AsyncLLMEngine(...)
    
    # 创建不同长度的音频样本
    audio_1 = torch.randn(48000)   # 1秒
    audio_2 = torch.randn(96000)   # 2秒  
    audio_3 = torch.randn(144000)  # 3秒
    
    requests = [
        {"prompt": "描述这段音频", "audio": audio_1},
        {"prompt": "你听到了什么?", "audio": audio_2},
        {"prompt": "转录这段音频", "audio": audio_3},
    ]
    
    # 同时发送所有请求
    results = await asyncio.gather(*[
        engine.generate(req["prompt"], audio=req["audio"])
        for req in requests
    ])
    
    # 应该不会死锁
    for i, result in enumerate(results):
        print(f"请求 {i}: {result}")
```

## 总结

**核心问题**: Commit d0836d8 为了修复多音频输入形状对齐问题,但引入了新问题:
- ✗ 展平了批次维度,丢失请求边界
- ✗ 移除了 `aftercnn_lens` 参数,导致注意力掩码错误
- ✗ 下游阶段接收到错位的特征
- ✗ Code2Wav 接收到长度为 1 的输入(不能被16整除)
- ✗ 并发请求场景下流水线死锁

**推荐操作**:
1. **立即**: 应用方案1,恢复 `aftercnn_lens` 参数并保持批次维度
2. **短期**: 添加验证和监控(方案2)
3. **长期**: 重新设计音频批处理以正确处理不同长度

**关键修复**: 恢复 `aftercnn_lens` 参数,不要展平批次维度。这样可以让 audio_tower 正确跟踪每个请求的序列边界,生成正确的注意力掩码,确保下游阶段接收到正确对齐的特征。
